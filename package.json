{
  "name": "abs-demo",
  "version": "1.0.0",
  "description": "swen 444 web browser project",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "electron",
    "browser",
    "webview",
    "react"
  ],
  "devDependencies": {
    "electron-prebuilt": "^0.28.2"
  },
  "dependencies": {
    "bootstrap": "^3.3.7",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-portal": "^3.2.0"
  }
}
